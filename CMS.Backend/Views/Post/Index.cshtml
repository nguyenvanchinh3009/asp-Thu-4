<<<<<<< HEAD
﻿@model IEnumerable<CMS.Data.Entities.Post>

@{
    Layout = "_LayoutAdmin";
    ViewData["Title"] = "Quản lý bài viết";
}

<div class="container mt-5">

    <!-- HEADER -->
    <div class="d-flex justify-content-between align-items-center mb-5 flex-wrap gap-3">

        <div>

            <h2 class="fw-bold text-uppercase mb-2"
                style="letter-spacing: 2px;">

                Quản lý bài viết

            </h2>

            <div class="bg-primary"
                 style="height: 3px; width: 70px;">
            </div>

            <p class="text-muted mt-2 mb-0">

                Tổng cộng có @(Model?.Count() ?? 0) bài viết

            </p>

        </div>

        <!-- BUTTON CREATE -->
        <a asp-action="Create"
           class="btn btn-primary rounded-pill px-4 shadow-sm">

            <i class="bi bi-plus-circle me-1"></i>

            Thêm bài viết mới

        </a>

    </div>

    <!-- POSTS -->
    <div class="row">

        @if (Model != null && Model.Any())
        {
            foreach (var item in Model)
            {
                <div class="col-lg-4 col-md-6 mb-4">

                    <div class="card h-100 border-0 shadow-sm post-card">

                        <!-- IMAGE -->
                        <div class="position-relative overflow-hidden post-image">

                            <img src="@(string.IsNullOrEmpty(item.ImageUrl)
                                                                         ? "https://via.placeholder.com/400x250?text=No+Image"
                                                                         : item.ImageUrl)"
                                                                                         class="w-100 h-100 transition-img"
                                                                                         style="object-fit:cover;"
                                                                                         alt="@item.Title" />

                            <!-- CATEGORY -->
                            <div class="category-badge">

                                @(item.Category != null
                                                        ? item.Category.Name
                                                        : "Không có danh mục")

                            </div>

                        </div>

                        <!-- BODY -->
                        <div class="card-body p-4">

                            <!-- CATEGORY -->
                            <span class="badge bg-info text-dark mb-3">

                                @(item.Category != null
                                                        ? item.Category.Name
                                                        : "Không có danh mục")

                    </span>

                            <!-- TITLE -->
                            <h5 class="card-title fw-bold text-dark line-clamp-2">

                                @item.Title

                            </h5>

                            <!-- CONTENT -->
                            <div class="card-text text-secondary small line-clamp-3 mb-4">

                                @Html.Raw(item.Content)

                            </div>

                            <!-- DATE -->
                            <p class="text-muted small mb-0">

                                <i class="bi bi-calendar-event me-1"></i>

                                @item.CreatedDate.ToString("dd/MM/yyyy")

                            </p>

                        </div>

                        <!-- FOOTER -->
                        <div class="card-footer bg-transparent border-0 p-4 pt-0">

                            <div class="d-grid gap-2">

                                <!-- DETAIL -->
                                <a asp-action="Details"
                                   asp-route-id="@item.Id"
                                   class="btn btn-dark rounded-pill py-2 fw-semibold shadow-sm">

                                    <i class="bi bi-eye me-1"></i>

                                    Đọc chi tiết

                                </a>

                                <!-- ACTION BUTTONS -->
                                <div class="d-flex gap-2">

                                    <!-- EDIT -->
                                    <a asp-action="Edit"
                                       asp-route-id="@item.Id"
                                       class="btn btn-outline-warning flex-fill rounded-pill">

                                        <i class="bi bi-pencil-square me-1"></i>

                                        Sửa

                                    </a>

                                    <!-- DELETE -->
                                    <a asp-action="Delete"
                                       asp-route-id="@item.Id"
                                       class="btn btn-outline-danger flex-fill rounded-pill"
                                       onclick="return confirm('Chúng ta có chắc chắn muốn xóa bài viết này không?')">

                                        <i class="bi bi-trash me-1"></i>

                                        Xóa

                                    </a>

                                </div>

                            </div>

                        </div>

                    </div>

                </div>
                }
        }
        else
        {
            <div class="col-12 text-center py-5">

                <i class="bi bi-folder-x display-1 text-light"></i>

                <p class="text-muted mt-3">

                    Hiện chưa có bài viết nào được đăng tải.

                </p>

            </div>
        }

    </div>

</div>

@section Scripts {

    <link rel="stylesheet"
          href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">

    <style>

        body {
            background: #f5f7fb;
        }

        .post-card {
            border-radius: 20px;
            transition: all 0.35s ease;
            overflow: hidden;
            background: #fff;
        }

            .post-card:hover {
                transform: translateY(-10px);
                box-shadow: 0 20px 40px rgba(0,0,0,0.12) !important;
            }

        .post-image {
            height: 230px;
        }

        .transition-img {
            transition: transform 0.7s ease;
        }

        .post-card:hover .transition-img {
            transform: scale(1.08);
        }

        .category-badge {
            position: absolute;
            top: 15px;
            left: 15px;
            background: rgba(78,115,223,0.95);
            color: white;
            padding: 6px 16px;
            border-radius: 50px;
            font-size: 11px;
            font-weight: bold;
            letter-spacing: 0.5px;
        }

        .line-clamp-2 {
            display: -webkit-box;
            -webkit-line-clamp: 2;
            -webkit-box-orient: vertical;
            overflow: hidden;
            min-height: 52px;
        }

        .line-clamp-3 {
            display: -webkit-box;
            -webkit-line-clamp: 3;
            -webkit-box-orient: vertical;
            overflow: hidden;
            min-height: 75px;
        }

        .btn-dark {
            background: #2c3e50;
            border: none;
            transition: all 0.3s ease;
        }

            .btn-dark:hover {
                background: #4e73df;
                transform: scale(1.02);
            }

        .btn-outline-warning:hover,
        .btn-outline-danger:hover {
            color: white;
        }

        .badge {
            font-size: 12px;
            padding: 8px 14px;
            border-radius: 50px;
        }
    </style>
}
=======
@model IEnumerable<CMS.Data.Entities.Post>

@{
    Layout = "_LayoutAdmin";
    ViewData["Title"] = "Danh Sách Bài Viết";
}

<div class="container mt-5">
    <div class="d-flex justify-content-between align-items-center mb-4 pb-2 border-bottom border-danger">
        <h2 class="mb-0 text-danger font-weight-bold text-uppercase tracking-wide">
            <i class="bi bi-journal-text me-2"></i>Danh Sách Bài Viết
        </h2>
        <div>
            <a asp-action="Create" class="btn btn-success me-3">
                <i class="bi bi-plus-circle"></i> Thêm bài viết mới
            </a>
            <span class="badge bg-secondary px-3 py-2 fs-6">Tổng số: @Model.Count() bài viết</span>
        </div>
    </div>

    @if (!Model.Any())
    {
        <div class="alert alert-warning text-center py-5 shadow-sm rounded-lg" role="alert">
            <i class="bi bi-exclamation-triangle-fill fs-1 text-warning mb-3 d-block"></i>
            <h4 class="alert-heading">Không có bài viết nào!</h4>
            <p class="mb-0 text-muted">Chưa có bài viết nào thuộc danh mục này trong cơ sở dữ liệu.</p>
            <a href="/Category" class="btn btn-primary mt-3">
                <i class="bi bi-arrow-left me-1"></i> Quay lại Danh mục
            </a>
        </div>
    }
    else
    {
        <div class="row">
            @foreach (var item in Model)
            {
                <div class="col-md-4 mb-4">
                    <div class="card h-100 border-0 shadow-sm transition-card hover-shadow">
                        <div class="position-relative">
                            @if (!string.IsNullOrEmpty(item.ImageUrl))
                            {
                                <img src="@item.ImageUrl" class="card-img-top" alt="@item.Title" style="height: 220px; object-fit: cover;">
                            }
                            else
                            {
                                <div class="bg-light d-flex align-items-center justify-content-center" style="height: 220px;">
                                    <i class="bi bi-image text-muted" style="font-size: 3rem;"></i>
                                </div>
                            }
                            <span class="position-absolute top-0 start-0 m-3 badge bg-info text-dark px-3 py-2 shadow-sm font-weight-bold">
                                <i class="bi bi-tag-fill me-1"></i>@item.Category?.Name
                            </span>
                        </div>
                        <div class="card-body d-flex flex-column p-4">
                            <h5 class="card-title text-primary font-weight-bold mb-3 line-clamp-2" style="height: 3rem; overflow: hidden;">
                                @item.Title
                            </h5>
                            <p class="card-text text-muted mb-4 line-clamp-3" style="height: 4.5rem; overflow: hidden; font-size: 0.95rem;">
                                @item.Content
                            </p>
                            <div class="mt-auto pt-3 border-top border-light">
                                <div class="d-flex justify-content-between align-items-center flex-wrap gap-2">
                                    <small class="text-muted me-2">
                                        <i class="bi bi-calendar3 me-1"></i> @item.CreatedDate.ToString("dd/MM/yyyy")
                                    </small>
                                    <div class="btn-group" role="group">
                                        <a href="/Post/Details/@item.Id" class="btn btn-outline-primary btn-sm px-3 rounded-pill">
                                            Xem chi tiết
                                        </a>
                                        <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-outline-warning btn-sm">
                                            Sửa
                                        </a>
                                        <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-outline-danger btn-sm" onclick="return confirm('Chúng ta có chắc chắn muốn xóa bài viết này không?')">
                                            <i class="bi bi-trash"></i>
                                        </a>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            }
        </div>
    }
</div>

<style>
    .transition-card {
        transition: transform 0.3s ease, box-shadow 0.3s ease;
    }
    .hover-shadow:hover {
        transform: translateY(-5px);
        box-shadow: 0 .5rem 1.5rem rgba(0,0,0,.15)!important;
    }
    .line-clamp-2 {
        display: -webkit-box;
        -webkit-line-clamp: 2;
        -webkit-box-orient: vertical;
        overflow: hidden;
    }
    .line-clamp-3 {
        display: -webkit-box;
        -webkit-line-clamp: 3;
        -webkit-box-orient: vertical;
        overflow: hidden;
    }
</style>
>>>>>>> bf8421308594fdc70a2f2729b121ae3dc4958834
